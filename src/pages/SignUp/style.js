import styled from 'styled-components/native'
import {
  PageContainerForm,
} from '@styles'

export const FormContainer = styled(PageContainerForm)`
  flex: 2;
  justify-content: center;
  padding-top: 15px;
`

export const NavigateTextButtonContainer = styled.View`
  margin-bottom: 20px;
`
